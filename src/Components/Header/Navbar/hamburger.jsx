import React from 'react';

const hamburger = ({ openSideDrawer }) => (
  <div className='em-hamburger' onClick={openSideDrawer}>
    <div />
    <div />
    <div />
  </div>
);

export default hamburger;
